PySide6>=6.8.0
aiohttp>=3.9
pynput>=1.7.6
pyautogui>=0.9.54
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26
Pillow>=10.2
mss>=9.0
pywin32>=306; platform_system == "Windows"

# STT + TTS — one library does both. sherpa-onnx covers Whisper /
# Paraformer / Zipformer / SenseVoice for STT, and Kokoro / VITS-Piper /
# Matcha-TTS for synthesis. Swap onnxruntime → onnxruntime-gpu for CUDA
# (setup.ps1 handles that when -GpuSupport is on).
sherpa-onnx>=1.10

# Model auto-download from HuggingFace (used by both engines when the
# user enters a repo ID instead of a local path).
huggingface_hub>=0.24
